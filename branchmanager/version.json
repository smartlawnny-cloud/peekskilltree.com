{
  "version": 228,
  "released": "2026-04-19",
  "notes": "Multi-tenant security: payments now scoped by tenant_id (was leaking across tenants). expenses + time_entries enforce tenant filter on cloud sync. Requires SQL migration to backfill tenant_id on those tables."
}
