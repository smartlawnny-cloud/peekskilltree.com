{
  "version": 311,
  "released": "2026-04-20",
  "notes": "Audit fixes: 1) _checkNewPayments respects open forms (same v310 pattern). 2) Logout tears down realtime subscription + polling timers + biometric session. 3) Biometric isEnabled self-heals when credId is missing. 4) authenticate() catches corrupted base64 + auto-disables. 5) Photos library tag chips use data attributes + delegated handlers (XSS-safe)."
}
