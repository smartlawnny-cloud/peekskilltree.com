{
  "version": 277,
  "released": "2026-04-19",
  "notes": "Restore FINAL FIX: drop _suspendAutoSave (was silently blocking all saves once banner appeared). Recovery data now snapshotted into a separate -recovery localStorage key so live autosave can't touch it."
}
