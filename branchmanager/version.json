{
  "version": 329,
  "released": "2026-04-21",
  "notes": "Nav settings: PWA and native App are now SEPARATE (each has its own Top/Bottom/Both choice — Settings shows two rows in the green box). Added bm-app-nav to CloudKeys sync list. Fixed a ReferenceError race where init crashed if ModeSelector script hadn't loaded yet."
}
