{
  "version": 316,
  "released": "2026-04-21",
  "notes": "Top-nav mode PWA/Capacitor: topbar padding-top=max(env(safe-area-inset-top),50px) so it always clears the iOS status bar. Bottom-nav mode untouched."
}
