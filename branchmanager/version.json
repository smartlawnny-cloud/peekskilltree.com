{
  "version": 290,
  "released": "2026-04-19",
  "notes": "Equipment map: mobile layout is now side-by-side (map LEFT, equipment list RIGHT in a 140px sidebar) instead of bottom drawer. Quote form has a new checkbox 'Show the equipment layout to the client on their approval page' (defaults ON, stored as q.showEquipMapToClient)."
}
