{
  "version": 292,
  "released": "2026-04-19",
  "notes": "SendGrid hardening: retry once on 5xx/429, classify errors (401/403/400/429) with clear toast hints, add email format validation, support server-side SENDGRID_API_KEY env (useServerKey option), no automatic mailto on every failure (only on network error). Edge Function already supports env fallback."
}
