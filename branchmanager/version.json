{
  "version": 345,
  "released": "2026-04-22",
  "notes": "Round 2 refactors: (#9) CompanyInfo.get() helper in config.js replaces 74+ scattered 'localStorage.getItem(bm-co-x) || BM_CONFIG.y' reads — one source of truth for company info. (#11) _resolveClientId() guard in DB.quotes/jobs/invoices/requests .create() auto-backfills clientId when clientName matches an existing client. Plus a new 'Reconcile Orphan Records' Settings button that scans the whole DB for orphans, shows a preview, and backfills links."
}
