{
  "version": 272,
  "released": "2026-04-19",
  "notes": "CRITICAL: bump-version.sh now bumps every ?v= cache-buster on script/link tags. Previously my autosave + restore + diagnostic fixes never reached the browser because quotes.js was stuck at ?v=246. Also manually bumped quotes.js + propertymap.js this ship."
}
