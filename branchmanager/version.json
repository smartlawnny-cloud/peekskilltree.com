{
  "version": 303,
  "released": "2026-04-20",
  "notes": "Nav spacing: REMOVED all env(safe-area-inset-top) math from topbar + content rules. Let Capacitor contentInset:automatic + native StatusBar plugin handle status bar entirely. Topbar gets plain 8px/48px. Bottom-nav mode keeps only bottom padding for the fixed bar."
}
