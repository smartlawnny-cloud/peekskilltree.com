{
  "version": 357,
  "released": "2026-04-23",
  "notes": "Anti-fabrication guardrails after 5 hallucinated Jobber-client rows were deleted. DB-layer: added import_source and created_by_agent columns on clients/quotes/jobs/invoices; CHECK constraint on import_source allowlist; trigger rejects inserts where notes claim 'pulled from/imported from' but import_source is NULL. UI-layer: new 'Audit AI-Created Data' button in Settings → Data Management lists every row any Claude session has added. Memory: added NEVER-fabricate rule at top of MEMORY.md so future sessions read it first."
}
