{
  "version": 239,
  "released": "2026-04-19",
  "notes": "Cloud Settings Sync — API keys and prefs auto-sync per tenant via new tenant_settings table. Set on one device, every device gets it on next login. Requires SQL migration."
}
