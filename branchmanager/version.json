{
  "version": 307,
  "released": "2026-04-20",
  "notes": "Biometric lock (Face ID / Touch ID / Windows Hello) via WebAuthn. Settings → Security → 🔐 toggle. One tap to enroll, then lock screen on every app open until authenticated. Session is per-tab (unlocks for that session only)."
}
