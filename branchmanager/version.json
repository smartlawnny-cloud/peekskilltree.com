{
  "version": 308,
  "released": "2026-04-20",
  "notes": "XSS hardening: photo viewer buttons now use addEventListener (no string interpolation of recordType/recordId). Recovery banner escapes clientName with UI.esc."
}
