{
  "version": 344,
  "released": "2026-04-22",
  "notes": "Round 1 safety pass — (#5) bmSafeCall helper + unhandledrejection safety net in supabase.js, visible toasts on CloudSync update/delete failures (previously silent). (#12) All 34 console.log calls → console.debug (hidden by default in Chrome/Safari devtools unless verbose is on, still available when debugging)."
}
