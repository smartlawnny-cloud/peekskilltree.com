{
  "version": 281,
  "released": "2026-04-19",
  "notes": "Schedule: unscheduled panel now accepts DROP from calendar — drag a scheduled job onto it to clear its scheduled date. Panel always visible (even empty) so it's a drop target."
}
