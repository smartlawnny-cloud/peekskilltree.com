{
  "version": 296,
  "released": "2026-04-20",
  "notes": "Bottom nav mode: detect Capacitor iOS app (window.Capacitor) as standalone, not just PWA install signals. Bottom nav now activates correctly inside the wrapped native app."
}
