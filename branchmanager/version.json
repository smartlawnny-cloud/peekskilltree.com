{
  "version": 327,
  "released": "2026-04-21",
  "notes": "Cross-device API-key sync now actually works. (1) Shipped migrate-tenant-settings.sql — the CloudKeys code expected this table but it was never created, so sync was silently failing. (2) CloudKeys now subscribes to Supabase realtime — a key saved on one device propagates to every other logged-in device instantly (no reload needed)."
}
