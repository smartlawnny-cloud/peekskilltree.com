{
  "version": 229,
  "released": "2026-04-19",
  "notes": "Photos now sync across devices via Supabase Storage + photos metadata table. Upload writes to job-photos bucket, metadata row carries tenant_id. App boot pulls all tenant photos into local cache. Label/delete also sync."
}
