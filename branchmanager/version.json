{
  "version": 338,
  "released": "2026-04-22",
  "notes": "Claude key can now live ONLY on Supabase. Settings → AI Assistant has a Server-managed/Device toggle. When server-managed: the key is set once via 'supabase secrets set ANTHROPIC_API_KEY=…' and no device ever stores or sees it. The ai-chat edge function prefers env var over client-sent key. All 5 client callers (Settings Test, AI Assistant, Quotes AI, Voice Quote, Video Quote, Tree ID) updated via window.bmClaudeKey() helper. bm-claude-server-managed setting syncs across devices via CloudKeys."
}
